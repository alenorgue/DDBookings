// UserRepository.js
// Interfaz del repositorio del dominio. Define los métodos necesarios.

class UserRepository {
  async findByEmail(email) {}
  async save(user) {}
}
export default UserRepository;
