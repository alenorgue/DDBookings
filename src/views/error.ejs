<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Error</title>
  <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@2.0.6/css/pico.min.css">
</head>
<body>
    <%- include('partials/navbar', { user: typeof user !== 'undefined' ? user : null }) %>
  <main class="container">
    <h1 style="color:#c00;">Ha ocurrido un error</h1>
    <p><%= message || 'Error inesperado' %></p>
    <% if (error) { %>
      <pre style="background:#f8d7da;color:#721c24;padding:1em;border-radius:6px;"><%= error %></pre>
    <% } %>
    <a href="/" class="button">Volver al inicio</a>
    <% if (user) { %>
      <a href="/dashboard/<%= user.id %>" class="button secondary">Ir a Mi panel</a>
    <% } %>
  </main>
</body>
</html>
