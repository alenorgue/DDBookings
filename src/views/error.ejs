<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Error</title>
  <link rel="icon" type="image/jpeg" href="/img/logo-header.jpg">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
  <link href="/css/views/error.css" rel="stylesheet">
</head>
<body>
  <div class="error-bg-overlay"></div>
  <%- include('partials/navbar', { user: typeof user !== 'undefined' ? user : null }) %>
  <main>
    <div class="error-card">
      <h1><i class="fa-solid fa-triangle-exclamation me-2"></i>Ha ocurrido un error</h1>
      <p><%= message || 'Error inesperado' %></p>
      <% if (error) { %>
        <pre><%= error %></pre>
      <% } %>
      <a href="/" class="btn btn-outline-primary"><i class="fa fa-home me-1"></i>Volver al inicio</a>
      <% if (user) { %>
        <a href="/dashboard/<%= user.id %>" class="btn btn-secondary"><i class="fa fa-user me-1"></i>Ir a Mi panel</a>
      <% } %>
    </div>
  </main>
  <%- include('partials/footer') %>
</body>
</html>
