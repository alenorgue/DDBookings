<nav style="width:100%;background:#fff;padding:0.35em 0;box-shadow:0 2px 8px rgba(0,0,0,0.07);margin-bottom:2em;position:sticky;top:0;z-index:100;">
  <div style="max-width:1200px;margin:0 auto;display:flex;align-items:center;justify-content:space-between;">
    <div style="display:flex;align-items:center;gap:1.5em;">
      <a href="/" style="font-weight:bold;font-size:1.15em;color:#2d3748;text-decoration:none;letter-spacing:0.5px;">🏠 Home</a>
      <a href="/accommodations" style="color:#4a5568;text-decoration:none;font-weight:500;transition:color 0.2s;">Alojamientos</a>
      <% if (user) { %>
        <a href="/dashboard/<%= user.id || user._id %>" style="color:#4a5568;text-decoration:none;font-weight:500;transition:color 0.2s;">Mi Panel</a>
        <a href="/createAccommodation" style="color:#4a5568;text-decoration:none;font-weight:500;transition:color 0.2s;">Añadir alojamiento</a>
        <% if (user.isAdmin) { %>
          <a href="/admin/dashboard" style="color:#3182ce;font-weight:bold;text-decoration:none;">Admin Panel</a>
        <% } %>
      <% } %>
    </div>
    <div class="navbar-right-block">
      <% if (user) { %>
        <form action="/auth/logout" method="POST" style="display:flex;align-items:center;margin:0;padding:0;">
          <button type="submit" class="logout-link">Cerrar sesión</button>
        </form>
      <% } else { %>
        <a href="/register" class="register-link" style="color:#4a5568;text-decoration:none;font-weight:500;">Registrar usuario</a>
        <a href="/login" style="color:#3182ce;text-decoration:none;font-weight:bold;">Iniciar sesión</a>
      <% } %>
    </div>
  </div>
</nav>
<style>
nav a:hover {
  color: #3182ce !important;
  text-decoration: underline;
}
.logout-link {
  background: none;
  border: none;
  color: #e53e3e;
  font-weight: bold;
  font-size: 1em;
  padding: 0;
  margin: 0 0 0 0.7em;
  cursor: pointer;
  transition: color 0.2s, text-decoration 0.2s;
  text-decoration: none;
  outline: none;
}
.logout-link:hover {
  color: #c53030;
  text-decoration: underline;
}
nav button:hover {
  background: none !important;
}
.navbar-right-block {
  display: flex;
  align-items: center;
  gap: 2.2em;
}
.register-link {
  margin-left: 2.5em;
}
@media (max-width: 700px) {
  nav div[style*='display:flex'] {
    flex-direction: column !important;
    align-items: flex-start !important;
    gap: 0.7em !important;
  }
  .navbar-right-block {
    width: 100%;
    justify-content: flex-start;
    margin-left: 0 !important;
    margin-top: 0.5em;
  }
}
</style>
